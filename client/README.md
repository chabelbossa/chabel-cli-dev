# Chabel CLI Auth Client

This folder contains the web authentication interface used by **Chabel CLI**.

## Run Locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in your browser.

## Notes

- The sign-in flow is used by the terminal command `chabel login`.
- Main auth UI entrypoint: `client/components/login-form.tsx`.
